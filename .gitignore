# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.venv/
venv/
__pycache__/
*.pyc

# Reference (third-party)
reference/stoooges/

# Claude settings (local only)
.claude/

# Verification/debug screenshots (throwaway)
reference/*.png

# Node deps (do not commit)
node_modules/

# Backend (Flask) runtime artifacts
backend/*.db
backend/instance/

# 公众号同步：access_token 凭据缓存 + 同步下载的封面（由服务器 cron 生成，不入库）
scripts/.wx_token_cache.json
frontend/assets/insights/

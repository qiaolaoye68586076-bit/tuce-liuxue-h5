# 途策留学 H5 — 设计素材 & 文案需求清单（你来设计）

## Context（为什么做这份清单）

H5 落地页框架（`/Users/qiaoyu/Downloads/tuce-liuxue-h5/`）已搭好，结构与版式保持不变。
本次只准备**素材 + 文案**：你按下面的清单把真实图片、文字、数据备齐交回，我负责把它们接入框架。
本次还有两处框架改动：① Hero 首屏改为**背景大图/视频**；② 新增一个**成功案例**图片板块。
配色、字体、版式沿用现框架，不重设。

---

## 一、全局规范（先看这块）

### 现有配色（保持，供你设计配图时取色）
| 名称 | 色值 | 用途 |
|---|---|---|
| 主色 深森林绿 | `#14342B` | 导航/数据区/深色块/页脚 |
| 次绿 | `#1E4A3C` | 渐变、师资区 |
| 暖金 强调 | `#C2A15B` | 按钮、强调、序号 |
| 浅金 | `#D9C28C` | 深色块上的标题/高亮 |
| 米白底 | `#FAF8F2` | 页面主背景 |
| 正文色 | `#20251F` | 文字 |

### 字体
系统字体栈（PingFang / 微软雅黑），**无需提供字体文件**。如需指定品牌字体请单独说明。

### 素材统一要求
- 所有图统一放进 `assets/` 目录，命名用清单里给出的**文件名**（我据此接入）。
- 位图请给 **2 倍图**（@2x），格式照片用 `jpg`（质量 80）、需透明用 `png`、矢量用 `svg`。
- 命名小写、用连字符，例：`hero-bg.jpg`、`mentor-01.jpg`。

---

## 二、按板块的素材 + 文案清单

> 标注 ⭐ = 必须重新提供；⬜ = 可沿用现有占位文案，想改再改。

### 0. 品牌 Logo ⭐（全站通用）
| 项 | 要求 |
|---|---|
| 主 Logo（深色版） | 当前使用 `logo.webp`（96×96，由原 logo-dark.svg 内嵌位图压缩而来，3.7KB）；用于浅色导航，显示高度约 34px |
| 反白 Logo（浅色版） | `logo-light.svg` / `logo-light.png`；用于页脚深绿底 |
| 站点图标 favicon | `favicon.png` 512×512（透明底）|
| 文字 Logo | 现为「途策留学 / TUCE EDUCATION」，确认中英文写法是否保留 |

### 1. Hero 首屏 ⭐（本次改为背景大图/视频）
- **背景图**：`hero-bg.jpg`，建议 **1920×1280**（横图，主体居中留四周安全区），或竖图 1500×2000；@2x、jpg 质量 80、≤500KB。
  - 题材建议：校园 / 名校建筑 / 学生场景 / 国际化氛围，**整体偏暗或可被压暗**（图上要叠文字）。
- **背景视频（可选，二选一）**：`hero-bg.mp4`，H.264、≤8 秒可循环、≤3MB、静音；1280×720 或 1080×1920。
  - 注：iOS 自动播放需静音 + playsinline（我会在代码里处理），并建议同时给一张 `hero-bg.jpg` 作首帧/降级图。
- 文案：
  - 小标题 eyebrow：现「高端一对一 · 留学全程规划」⬜
  - 主标题 ⭐：现「以策略，渡你抵达理想院校」（≤14 字/行，2 行内，可含 1 个高亮词）
  - 副标题 ⭐：现两行说明（≤40 字）
  - 两个按钮文字：现「免费定制方案」「了解服务 ↓」⬜

### 2. 核心数据 ⭐（4 个数字）
| 现值 | 需你确认真实数据 |
|---|---|
| 9 年深耕留学行业 | 成立年限 |
| 3000+ 累计录取 Offer | Offer 总数 |
| 120+ Top 名校导师 | 导师人数 |
| 98% 学员满意度 | 满意度 |
> 纯数字 + 后缀（如 `+`、`%`），无需图片。

### 3. 服务（6 张卡片）⬜
每张：标题（已定：美国本科/研究生/转学/英欧加港本科/单项服务/商务合作）+ 一句话描述（≤40 字）。
现有描述可直接用，想调整文案就给新文案。**无需配图**（卡片为文字+序号设计）。

### 4. 为什么选途策（4 条）⬜
每条：图标（现用 emoji 🎯🎓🤝🔍）+ 标题（4 字内）+ 描述（≤45 字）。
如想换成线性图标，提供 4 个 `why-01.svg`…`why-04.svg`（单色、24×24，可被改色）。

### 5. 申请流程（6 步）⬜
每步：标题（4 字）+ 描述（≤25 字）。现有文案可用。无需图片。

### 6. 师资 ⭐（4 位导师头像）
| 项 | 要求 |
|---|---|
| 头像 ⭐ | `mentor-01.jpg` … `mentor-04.jpg`，正方形 **256×256**，jpg；人物居中、风格统一 |
| 角色/院校文字 ⭐ | 每位：角色（如「规划导师」）+ 院校方向（如「哈佛大学 · 升学战略」）|
| 数量 | 默认 4 位；如要更多/更少告诉我，我调整栅格 |

### 7. 成功案例 ⭐（本次新增图片板块）
- 案例配图：`case-01.jpg` … （建议 3–6 个），**800×600（4:3）** 或 1000×750，jpg。
  - 内容可为：Offer 信封/录取页截图、学员故事配图、院校照片（注意隐私与版权）。
- 每个案例文案 ⭐：学员标签（如「W 同学」）+ 录取院校 + 一句话结果/感言（≤40 字）。
- 是否保留现有的 **Offer 院校跑马灯**（纯文字：Harvard/Yale…）？保留 / 换成校徽图 / 删除，三选一。
  - 提示：大学校徽多为注册商标，商用展示有合规风险，默认建议保留**文字**形式。

### 8. 留资表单 ⬜
字段已定（姓名/咨询方向/电话/微信/城市/学校/更多信息 + 协议勾选）。
- 需提供 ⭐：**用户协议 & 隐私政策**的正式链接或文案（现为弹窗占位）。
- 左侧引导文案 + 3 个卖点（现「1对1背景诊断 / 专属选校建议 / 无隐性收费」）⬜
- 后端留资接口地址（有就给，没有先空着走本地留底）。

### 9. 页脚 ⭐
| 项 | 要求 |
|---|---|
| 微信二维码 ⭐ | `qr-wechat.png`，**480×480**，png，四周留白（quiet zone），中心可带 Logo |
| 联系电话 ⭐ | 替换占位 400-000-0000 |
| 邮箱 ⭐ | 替换 hello@tuce.com |
| 工作时间 | 现「周一至周日 9:00–21:00」⬜ |
| ICP 备案号 ⭐ | 替换占位「沪ICP备 0000000 号」 |
| 版权署名 | 现「© 2026 途策留学」⬜ |

---

## 三、素材交付汇总表（你照这张表备齐文件）

| 文件名 | 尺寸/格式 | 板块 | 必需 |
|---|---|---|---|
| `logo.webp` | 96×96 WebP / 显示高 ~34px | 导航 | ⭐ |
| `logo-light.svg/png` | 同上反白 | 页脚 | ⭐ |
| `favicon.png` | 512×512 png | 站点图标 | ⭐ |
| `hero-bg.jpg` | 1920×1280 jpg ≤500KB | 首屏 | ⭐ |
| `hero-bg.mp4` | ≤8s ≤3MB 静音 | 首屏(可选) | ⬜ |
| `mentor-01~04.jpg` | 256×256 jpg | 师资 | ⭐ |
| `case-01~06.jpg` | 800×600 jpg | 成功案例 | ⭐ |
| `qr-wechat.png` | 480×480 png | 页脚 | ⭐ |
| `why-01~04.svg`（可选） | 24×24 单色 | 优势图标 | ⬜ |

**文字/数据清单（用文档给我即可）**：主标题、副标题、4 个真实数据、6 条服务描述（如改）、师资角色+院校 ×4、案例标签+院校+感言 ×N、协议链接、电话/邮箱/ICP、留资接口地址。

---

## 四、素材就位后我要做的接入改动（不需你操作，列出供知晓）

1. `index.html` / `css/style.css` — Hero `.hero__bg` 改为引用 `hero-bg.jpg`（或 `<video>` + 压暗蒙层），保证文字可读。
2. 导航 logo 现用 `logo.webp`（已接入）；页脚占位与 favicon 链接待补。
3. 师资 `.mentor__avatar` 首字母圈 → 改为 `<img>` 引用 `mentor-0x.jpg`。
4. 新增「成功案例」板块（图片卡片栅格）+ 锚点与导航项；按你的选择处理 Offer 跑马灯。
5. 页脚 `.qr` 占位框 → 替换为 `qr-wechat.png`；填入真实电话/邮箱/ICP。
6. 数据区 4 个数字、各板块文案按你的文档更新。
7. 协议弹窗 → 链接到正式协议页。

## 五、验证方式
- 本地：`cd tuce-liuxue-h5 && python3 -m http.server 8080`，浏览器开 `http://localhost:8080`，Chrome 切移动视图逐屏核对素材与文案。
- 手机：访问电脑局域网 IP 实机查看 Hero 大图/视频在 iOS Safari 的自动播放与压暗效果。
